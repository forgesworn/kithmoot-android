<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only.

  Release builds keep the platform default, which refuses cleartext outright:
  a ws:// relay would show a passive observer which room ids a device is
  talking about, even though it could not read a word of the traffic. During
  development a relay on the machine running the emulator is worth the trade,
  and only for that machine.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
