Synthetic FSWNENC2 fixtures generated by the current KithMoot TypeScript
`packages/context/src/blossom.ts` encryptEnvelope implementation. Keys in the
JSON files are fixture-only and protect no private data. web-image is a 640x360
red/blue PNG; web-multirecord is 1,100,000 bytes of decimal 73. Both use random
salts, nonces and padding, as production writers do. The reader tests consume
the captured bytes rather than reimplementing the writer in Kotlin.
